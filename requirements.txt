# Candidate Data Unification Pipeline — Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework
Flask==3.1.2
Werkzeug==3.1.3

# File parsing
python-docx==1.2.0        # DOCX resume parsing
lxml==6.1.1               # XML/HTML parsing (used by python-docx)
pdfplumber>=0.11.0        # PDF resume parsing

# HTTP / URL ingestion
requests==2.32.5          # GitHub & LinkedIn profile fetching

# Testing
pytest==9.1.1
